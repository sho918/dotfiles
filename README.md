# Dotfiles

```bash
# Install homebrew
$ /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Install tools
$ brew install asdf diff-so-fancy direnv fish fzf ghq tmux
```
